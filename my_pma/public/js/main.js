DocumentLoad(main);
function main()
{
	/*
	eff = new Effect();
	eff.odd();
	*/
	elements = $$("tbody")[0];
	
}
function debug(obj)
{
		params = "";
		for (param in obj)
			params = params + param + "\n";
		alert(params);
}

